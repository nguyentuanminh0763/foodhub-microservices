// ============================================================================
// TODO (YOU CODE THIS) — map URLs to controller functions
// ----------------------------------------------------------------------------
// const express = require("express");
// const router = express.Router();
// const c = require("../controllers/restaurant.controller");
// router.get("/",  c.listRestaurants);
// router.get("/:id", c.getRestaurant);
// router.post("/", c.createRestaurant);
// ... thêm route cho getDish
// module.exports = router;
//
// Lưu ý: index.js đã mount router này ở prefix /api/restaurants, nên ở đây chỉ
// khai báo phần đường dẫn phía sau ("/", "/:id", ...).
// ============================================================================
const express = require("express");
const router = express.Router();

// define your routes here

module.exports = router;
