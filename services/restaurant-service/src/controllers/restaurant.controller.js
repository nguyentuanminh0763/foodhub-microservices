// ============================================================================
// TODO (YOU CODE THIS) — controller functions
// ----------------------------------------------------------------------------
// Mỗi hàm nhận (req, res), thao tác với model Restaurant, trả JSON. Cần:
//   listRestaurants(req,res)         -> GET danh sách nhà hàng
//   getRestaurant(req,res)           -> GET 1 nhà hàng theo id
//   createRestaurant(req,res)        -> POST tạo mới
//   getDish(req,res)                 -> GET 1 món (dùng cho Order gọi sang kiểm tra giá)
// Nhớ try/catch và trả mã lỗi hợp lý (404 khi không thấy, 400 khi body sai).
// ============================================================================

// const Restaurant = require("../models/Restaurant");
// exports.listRestaurants = async (req, res) => { ... };
