{
  "extends": "./tsconfig.json",
  // Without excluding prisma.config.ts, `nest build` emits a prisma.config.js
  // that the Prisma CLI then loads instead of the .ts.
  "exclude": ["node_modules", "dist", "prisma.config.ts", "**/*.spec.ts"]
}
